name: Universal Release Forge

on:
  push:
    tags:
      - 'v*' # Triggers the cloud build automatically whenever you push a version tag like v1.0.1

jobs:
  forge-assets:
    permissions:
      contents: write
    runs-on: ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        platform: [windows-latest, macos-latest, ubuntu-latest]

    steps:
      - name: Checkout Code Base
        uses: actions/checkout@v4

      - name: Initialize Rust Environment
        uses: dtolnay/rust-toolchain@stable

      - name: Run Build Sequence
        run: cargo build --release

      - name: Upload Binaries to Release Hub
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            target/release/sentinel_agent*
            target/release/sentinel_core*
